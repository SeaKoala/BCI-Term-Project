%% Best Features
function [bestFeat] = findBestFeats(len, beforetrig, aftertrig, x)
for(s =1:length(x(1,1,:)))
    for(f = 1:length(x(:,1,1)))
        for(c = 1:length(x(1,:,1)))
            if(x(f,c,s) >1)
                y(end+1) = [
index = find(x >1);
bestFeat.weight = x(index);
bestFeat.frame = mod(index,len);
for i=1:length( bestFeat.frame)
if bestFeat.frame(i) == 0;
    bestFeat.frame(i) = 7;
end
bestFeat.channel =ceil(index/len);
end